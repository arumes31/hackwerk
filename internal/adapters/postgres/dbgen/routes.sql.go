// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: routes.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const deleteRouteStops = `-- name: DeleteRouteStops :exec
DELETE FROM route_stops
WHERE route_draft_id=$1::uuid
`

func (q *Queries) DeleteRouteStops(ctx context.Context, routeDraftID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteRouteStops, routeDraftID)
	return err
}

const getRouteDraft = `-- name: GetRouteDraft :one
SELECT rd.id::text, rd.actor_user_id::text, rd.driver_id::text, d.display_name AS driver_name,
       COALESCE(rd.chipper_resource_id::text, '')::text AS rd_chipper_resource_id,
       COALESCE(chipper.name, '')::text AS chipper_name,
       COALESCE(rd.transport_resource_id::text, '')::text AS transport_resource_id,
       COALESCE(transport.name, '')::text AS transport_name,
       rd.departure_at, rd.start_label, rd.start_latitude::text, rd.start_longitude::text,
       rd.end_label, rd.end_latitude::text, rd.end_longitude::text, rd.status, rd.routing_source,
       rd.distance_meters, rd.duration_seconds, rd.route_geometry, rd.assigned_at,
       rd.version, rd.created_at, rd.updated_at
FROM route_drafts rd
JOIN drivers d ON d.id=rd.driver_id
LEFT JOIN resources chipper ON chipper.id=rd.chipper_resource_id
LEFT JOIN resources transport ON transport.id=rd.transport_resource_id
WHERE rd.id=$1::uuid
`

type GetRouteDraftRow struct {
	RdID                string
	RdActorUserID       string
	RdDriverID          string
	DriverName          string
	RdChipperResourceID string
	ChipperName         string
	TransportResourceID string
	TransportName       string
	DepartureAt         pgtype.Timestamptz
	StartLabel          string
	RdStartLatitude     string
	RdStartLongitude    string
	EndLabel            string
	RdEndLatitude       string
	RdEndLongitude      string
	Status              string
	RoutingSource       string
	DistanceMeters      int32
	DurationSeconds     int32
	RouteGeometry       []byte
	AssignedAt          pgtype.Timestamptz
	Version             int32
	CreatedAt           pgtype.Timestamptz
	UpdatedAt           pgtype.Timestamptz
}

func (q *Queries) GetRouteDraft(ctx context.Context, id pgtype.UUID) (GetRouteDraftRow, error) {
	row := q.db.QueryRow(ctx, getRouteDraft, id)
	var i GetRouteDraftRow
	err := row.Scan(
		&i.RdID,
		&i.RdActorUserID,
		&i.RdDriverID,
		&i.DriverName,
		&i.RdChipperResourceID,
		&i.ChipperName,
		&i.TransportResourceID,
		&i.TransportName,
		&i.DepartureAt,
		&i.StartLabel,
		&i.RdStartLatitude,
		&i.RdStartLongitude,
		&i.EndLabel,
		&i.RdEndLatitude,
		&i.RdEndLongitude,
		&i.Status,
		&i.RoutingSource,
		&i.DistanceMeters,
		&i.DurationSeconds,
		&i.RouteGeometry,
		&i.AssignedAt,
		&i.Version,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertRouteDraft = `-- name: InsertRouteDraft :one
INSERT INTO route_drafts (
    actor_user_id, driver_id, chipper_resource_id, transport_resource_id,
    departure_at, start_label, start_latitude, start_longitude, end_label, end_latitude, end_longitude,
    routing_source, distance_meters, duration_seconds, route_geometry
) VALUES (
    $1::uuid, $2::uuid, $3::uuid,
    NULLIF($4::text, '')::uuid,
    $5::timestamptz, $6, $7::numeric,
    $8::numeric, $9, $10::numeric,
    $11::numeric, $12,
    $13, $14, $15::jsonb
)
RETURNING id::text, version
`

type InsertRouteDraftParams struct {
	ActorUserID         pgtype.UUID
	DriverID            pgtype.UUID
	ChipperResourceID   pgtype.UUID
	TransportResourceID string
	DepartureAt         pgtype.Timestamptz
	StartLabel          string
	StartLatitude       pgtype.Numeric
	StartLongitude      pgtype.Numeric
	EndLabel            string
	EndLatitude         pgtype.Numeric
	EndLongitude        pgtype.Numeric
	RoutingSource       string
	DistanceMeters      int32
	DurationSeconds     int32
	RouteGeometry       []byte
}

type InsertRouteDraftRow struct {
	ID      string
	Version int32
}

func (q *Queries) InsertRouteDraft(ctx context.Context, arg InsertRouteDraftParams) (InsertRouteDraftRow, error) {
	row := q.db.QueryRow(ctx, insertRouteDraft,
		arg.ActorUserID,
		arg.DriverID,
		arg.ChipperResourceID,
		arg.TransportResourceID,
		arg.DepartureAt,
		arg.StartLabel,
		arg.StartLatitude,
		arg.StartLongitude,
		arg.EndLabel,
		arg.EndLatitude,
		arg.EndLongitude,
		arg.RoutingSource,
		arg.DistanceMeters,
		arg.DurationSeconds,
		arg.RouteGeometry,
	)
	var i InsertRouteDraftRow
	err := row.Scan(&i.ID, &i.Version)
	return i, err
}

const insertRouteStop = `-- name: InsertRouteStop :one
INSERT INTO route_stops (
    route_draft_id, job_id, job_version, waitlist_version, position, travel_distance_meters,
    travel_duration_seconds, planned_starts_at, planned_ends_at
) VALUES (
    $1::uuid, $2::uuid, $3, $4, $5,
    $6, $7,
    $8::timestamptz, $9::timestamptz
)
RETURNING id::text
`

type InsertRouteStopParams struct {
	RouteDraftID          pgtype.UUID
	JobID                 pgtype.UUID
	JobVersion            int32
	WaitlistVersion       int32
	Position              int32
	TravelDistanceMeters  int32
	TravelDurationSeconds int32
	PlannedStartsAt       pgtype.Timestamptz
	PlannedEndsAt         pgtype.Timestamptz
}

func (q *Queries) InsertRouteStop(ctx context.Context, arg InsertRouteStopParams) (string, error) {
	row := q.db.QueryRow(ctx, insertRouteStop,
		arg.RouteDraftID,
		arg.JobID,
		arg.JobVersion,
		arg.WaitlistVersion,
		arg.Position,
		arg.TravelDistanceMeters,
		arg.TravelDurationSeconds,
		arg.PlannedStartsAt,
		arg.PlannedEndsAt,
	)
	var id string
	err := row.Scan(&id)
	return id, err
}

const latestAssignedRouteForDriver = `-- name: LatestAssignedRouteForDriver :one
SELECT id::text
FROM route_drafts
WHERE driver_id=$1::uuid AND status='assigned'
  AND (departure_at AT TIME ZONE 'Europe/Vienna')::date=$2::date
ORDER BY departure_at DESC, id DESC
LIMIT 1
`

type LatestAssignedRouteForDriverParams struct {
	DriverID  pgtype.UUID
	LocalDate pgtype.Date
}

func (q *Queries) LatestAssignedRouteForDriver(ctx context.Context, arg LatestAssignedRouteForDriverParams) (string, error) {
	row := q.db.QueryRow(ctx, latestAssignedRouteForDriver, arg.DriverID, arg.LocalDate)
	var id string
	err := row.Scan(&id)
	return id, err
}

const linkRouteStopAppointment = `-- name: LinkRouteStopAppointment :execrows
UPDATE route_stops
SET appointment_id=$1::uuid
WHERE id=$2::uuid AND route_draft_id=$3::uuid
  AND appointment_id IS NULL
`

type LinkRouteStopAppointmentParams struct {
	AppointmentID pgtype.UUID
	ID            pgtype.UUID
	RouteDraftID  pgtype.UUID
}

func (q *Queries) LinkRouteStopAppointment(ctx context.Context, arg LinkRouteStopAppointmentParams) (int64, error) {
	result, err := q.db.Exec(ctx, linkRouteStopAppointment, arg.AppointmentID, arg.ID, arg.RouteDraftID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const listDraftRouteIDsForDate = `-- name: ListDraftRouteIDsForDate :many
SELECT id::text
FROM route_drafts
WHERE status='draft'
  AND (departure_at AT TIME ZONE 'Europe/Vienna')::date=$1::date
ORDER BY departure_at, driver_id, id
LIMIT 50
`

func (q *Queries) ListDraftRouteIDsForDate(ctx context.Context, localDate pgtype.Date) ([]string, error) {
	rows, err := q.db.Query(ctx, listDraftRouteIDsForDate, localDate)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []string{}
	for rows.Next() {
		var id string
		if err := rows.Scan(&id); err != nil {
			return nil, err
		}
		items = append(items, id)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRouteCandidates = `-- name: ListRouteCandidates :many
SELECT j.id::text AS job_id, j.job_number, j.version AS job_version, w.version AS waitlist_version, j.workflow_status,
       j.job_type, j.transport_mode, j.external_transport_confirmed,
       j.estimated_hack_minutes, j.estimated_transport_minutes, j.volume_m3::text,
       COALESCE(NULLIF(j.region, ''), NULLIF(c.locality, ''), '')::text AS region,
       concat_ws(' ', NULLIF(c.first_name, ''), NULLIF(c.last_name, ''), NULLIF(c.company_name, ''))::text AS customer_name,
       c.locality,
       j.pile_latitude::text AS latitude, j.pile_longitude::text AS longitude,
       j.pile_location_source,
       EXISTS (
           SELECT 1 FROM appointments active_appointment
           WHERE active_appointment.job_id=j.id
             AND active_appointment.lifecycle_status IN ('draft', 'proposal', 'fixed')
       ) AS has_active_appointment
FROM jobs j
JOIN customers c ON c.id=j.customer_id
JOIN waitlist_entries w ON w.job_id=j.id AND w.removed_at IS NULL
WHERE j.archived_at IS NULL AND c.archived_at IS NULL
  AND j.workflow_status IN ('waitlist', 'planning')
  AND j.pile_latitude IS NOT NULL AND j.pile_longitude IS NOT NULL
  AND (
      cardinality($1::uuid[]) = 0
      OR (
          j.id=ANY($1::uuid[])
          AND NOT EXISTS (
              SELECT 1 FROM appointments selected_appointment
              WHERE selected_appointment.job_id=j.id
                AND selected_appointment.lifecycle_status IN ('draft', 'proposal', 'fixed')
          )
      )
  )
ORDER BY j.received_at, j.id
LIMIT 500
`

type ListRouteCandidatesRow struct {
	JobID                      string
	JobNumber                  string
	JobVersion                 int32
	WaitlistVersion            int32
	WorkflowStatus             string
	JobType                    string
	TransportMode              string
	ExternalTransportConfirmed bool
	EstimatedHackMinutes       int32
	EstimatedTransportMinutes  int32
	JVolumeM3                  string
	Region                     string
	CustomerName               string
	Locality                   string
	Latitude                   string
	Longitude                  string
	PileLocationSource         *string
	HasActiveAppointment       bool
}

func (q *Queries) ListRouteCandidates(ctx context.Context, jobIds []pgtype.UUID) ([]ListRouteCandidatesRow, error) {
	rows, err := q.db.Query(ctx, listRouteCandidates, jobIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRouteCandidatesRow{}
	for rows.Next() {
		var i ListRouteCandidatesRow
		if err := rows.Scan(
			&i.JobID,
			&i.JobNumber,
			&i.JobVersion,
			&i.WaitlistVersion,
			&i.WorkflowStatus,
			&i.JobType,
			&i.TransportMode,
			&i.ExternalTransportConfirmed,
			&i.EstimatedHackMinutes,
			&i.EstimatedTransportMinutes,
			&i.JVolumeM3,
			&i.Region,
			&i.CustomerName,
			&i.Locality,
			&i.Latitude,
			&i.Longitude,
			&i.PileLocationSource,
			&i.HasActiveAppointment,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRouteDrivers = `-- name: ListRouteDrivers :many
SELECT id::text, display_name, version
FROM drivers
WHERE active AND can_complete_jobs
ORDER BY lower(display_name), id
`

type ListRouteDriversRow struct {
	ID          string
	DisplayName string
	Version     int32
}

func (q *Queries) ListRouteDrivers(ctx context.Context) ([]ListRouteDriversRow, error) {
	rows, err := q.db.Query(ctx, listRouteDrivers)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRouteDriversRow{}
	for rows.Next() {
		var i ListRouteDriversRow
		if err := rows.Scan(&i.ID, &i.DisplayName, &i.Version); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRouteMissingLocations = `-- name: ListRouteMissingLocations :many
SELECT j.id::text AS job_id, j.job_number,
       concat_ws(' ', NULLIF(c.first_name, ''), NULLIF(c.last_name, ''), NULLIF(c.company_name, ''))::text AS customer_name,
       COALESCE(NULLIF(j.region, ''), NULLIF(c.locality, ''), '')::text AS region
FROM jobs j
JOIN customers c ON c.id=j.customer_id
JOIN waitlist_entries w ON w.job_id=j.id AND w.removed_at IS NULL
WHERE j.archived_at IS NULL AND c.archived_at IS NULL
  AND j.workflow_status IN ('waitlist', 'planning')
  AND (j.pile_latitude IS NULL OR j.pile_longitude IS NULL)
ORDER BY j.received_at, j.id
LIMIT 100
`

type ListRouteMissingLocationsRow struct {
	JobID        string
	JobNumber    string
	CustomerName string
	Region       string
}

func (q *Queries) ListRouteMissingLocations(ctx context.Context) ([]ListRouteMissingLocationsRow, error) {
	rows, err := q.db.Query(ctx, listRouteMissingLocations)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRouteMissingLocationsRow{}
	for rows.Next() {
		var i ListRouteMissingLocationsRow
		if err := rows.Scan(
			&i.JobID,
			&i.JobNumber,
			&i.CustomerName,
			&i.Region,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRouteResources = `-- name: ListRouteResources :many
SELECT id::text, name, resource_type, exclusive, version
FROM resources
WHERE active AND resource_type IN ('chipper', 'transport_vehicle')
ORDER BY resource_type, lower(name), id
`

type ListRouteResourcesRow struct {
	ID           string
	Name         string
	ResourceType string
	Exclusive    bool
	Version      int32
}

func (q *Queries) ListRouteResources(ctx context.Context) ([]ListRouteResourcesRow, error) {
	rows, err := q.db.Query(ctx, listRouteResources)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRouteResourcesRow{}
	for rows.Next() {
		var i ListRouteResourcesRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.ResourceType,
			&i.Exclusive,
			&i.Version,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRouteStopPhones = `-- name: ListRouteStopPhones :many
SELECT j.id::text AS job_id, COALESCE(c.phone_raw, '')::text AS customer_phone
FROM jobs j
JOIN customers c ON c.id=j.customer_id
WHERE j.id=ANY($1::uuid[])
`

type ListRouteStopPhonesRow struct {
	JobID         string
	CustomerPhone string
}

func (q *Queries) ListRouteStopPhones(ctx context.Context, jobIds []pgtype.UUID) ([]ListRouteStopPhonesRow, error) {
	rows, err := q.db.Query(ctx, listRouteStopPhones, jobIds)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRouteStopPhonesRow{}
	for rows.Next() {
		var i ListRouteStopPhonesRow
		if err := rows.Scan(&i.JobID, &i.CustomerPhone); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listRouteStops = `-- name: ListRouteStops :many
SELECT rs.id::text, rs.route_draft_id::text, rs.job_id::text, rs.job_version, rs.waitlist_version, rs.position,
       rs.travel_distance_meters, rs.travel_duration_seconds,
       rs.planned_starts_at, rs.planned_ends_at,
       COALESCE(rs.appointment_id::text, '')::text AS appointment_id,
       j.job_number, j.job_type, j.transport_mode, j.external_transport_confirmed,
       j.estimated_hack_minutes, j.estimated_transport_minutes, j.volume_m3::text,
       COALESCE(NULLIF(j.region, ''), NULLIF(c.locality, ''), '')::text AS region,
       concat_ws(' ', NULLIF(c.first_name, ''), NULLIF(c.last_name, ''), NULLIF(c.company_name, ''))::text AS customer_name,
       c.locality, COALESCE(j.pile_latitude::text, '')::text AS latitude,
       COALESCE(j.pile_longitude::text, '')::text AS longitude,
       COALESCE(j.pile_location_source, '')::text AS pile_location_source
FROM route_stops rs
JOIN jobs j ON j.id=rs.job_id
JOIN customers c ON c.id=j.customer_id
WHERE rs.route_draft_id=$1::uuid
ORDER BY rs.position, rs.id
`

type ListRouteStopsRow struct {
	RsID                       string
	RsRouteDraftID             string
	RsJobID                    string
	JobVersion                 int32
	WaitlistVersion            int32
	Position                   int32
	TravelDistanceMeters       int32
	TravelDurationSeconds      int32
	PlannedStartsAt            pgtype.Timestamptz
	PlannedEndsAt              pgtype.Timestamptz
	AppointmentID              string
	JobNumber                  string
	JobType                    string
	TransportMode              string
	ExternalTransportConfirmed bool
	EstimatedHackMinutes       int32
	EstimatedTransportMinutes  int32
	JVolumeM3                  string
	Region                     string
	CustomerName               string
	Locality                   string
	Latitude                   string
	Longitude                  string
	PileLocationSource         string
}

func (q *Queries) ListRouteStops(ctx context.Context, routeDraftID pgtype.UUID) ([]ListRouteStopsRow, error) {
	rows, err := q.db.Query(ctx, listRouteStops, routeDraftID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListRouteStopsRow{}
	for rows.Next() {
		var i ListRouteStopsRow
		if err := rows.Scan(
			&i.RsID,
			&i.RsRouteDraftID,
			&i.RsJobID,
			&i.JobVersion,
			&i.WaitlistVersion,
			&i.Position,
			&i.TravelDistanceMeters,
			&i.TravelDurationSeconds,
			&i.PlannedStartsAt,
			&i.PlannedEndsAt,
			&i.AppointmentID,
			&i.JobNumber,
			&i.JobType,
			&i.TransportMode,
			&i.ExternalTransportConfirmed,
			&i.EstimatedHackMinutes,
			&i.EstimatedTransportMinutes,
			&i.JVolumeM3,
			&i.Region,
			&i.CustomerName,
			&i.Locality,
			&i.Latitude,
			&i.Longitude,
			&i.PileLocationSource,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockRouteDraft = `-- name: LockRouteDraft :one
SELECT rd.id::text, rd.driver_id::text,
       COALESCE(rd.chipper_resource_id::text, '')::text AS rd_chipper_resource_id,
       COALESCE(rd.transport_resource_id::text, '')::text AS transport_resource_id,
       rd.departure_at, rd.duration_seconds, rd.status, rd.version
FROM route_drafts rd
WHERE rd.id=$1::uuid
FOR UPDATE
`

type LockRouteDraftRow struct {
	RdID                string
	RdDriverID          string
	RdChipperResourceID string
	TransportResourceID string
	DepartureAt         pgtype.Timestamptz
	DurationSeconds     int32
	Status              string
	Version             int32
}

func (q *Queries) LockRouteDraft(ctx context.Context, id pgtype.UUID) (LockRouteDraftRow, error) {
	row := q.db.QueryRow(ctx, lockRouteDraft, id)
	var i LockRouteDraftRow
	err := row.Scan(
		&i.RdID,
		&i.RdDriverID,
		&i.RdChipperResourceID,
		&i.TransportResourceID,
		&i.DepartureAt,
		&i.DurationSeconds,
		&i.Status,
		&i.Version,
	)
	return i, err
}

const lockRouteStopsForAssignment = `-- name: LockRouteStopsForAssignment :many
SELECT rs.id::text, rs.job_id::text, rs.job_version, rs.waitlist_version, rs.position,
       rs.travel_duration_seconds, rs.planned_starts_at, rs.planned_ends_at,
       j.version AS current_job_version, j.workflow_status, j.archived_at,
       j.job_type, j.transport_mode, j.external_transport_confirmed,
       COALESCE(j.pile_latitude::text, '')::text AS latitude,
       COALESCE(j.pile_longitude::text, '')::text AS longitude,
       COALESCE(w.id::text, '')::text AS waitlist_id, COALESCE(w.version, 0)::integer AS current_waitlist_version
FROM route_stops rs
JOIN jobs j ON j.id=rs.job_id
JOIN waitlist_entries w ON w.job_id=j.id AND w.removed_at IS NULL
WHERE rs.route_draft_id=$1::uuid
ORDER BY rs.position, rs.id
FOR UPDATE OF rs, j, w
`

type LockRouteStopsForAssignmentRow struct {
	RsID                       string
	RsJobID                    string
	JobVersion                 int32
	WaitlistVersion            int32
	Position                   int32
	TravelDurationSeconds      int32
	PlannedStartsAt            pgtype.Timestamptz
	PlannedEndsAt              pgtype.Timestamptz
	CurrentJobVersion          int32
	WorkflowStatus             string
	ArchivedAt                 pgtype.Timestamptz
	JobType                    string
	TransportMode              string
	ExternalTransportConfirmed bool
	Latitude                   string
	Longitude                  string
	WaitlistID                 string
	CurrentWaitlistVersion     int32
}

func (q *Queries) LockRouteStopsForAssignment(ctx context.Context, routeDraftID pgtype.UUID) ([]LockRouteStopsForAssignmentRow, error) {
	rows, err := q.db.Query(ctx, lockRouteStopsForAssignment, routeDraftID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []LockRouteStopsForAssignmentRow{}
	for rows.Next() {
		var i LockRouteStopsForAssignmentRow
		if err := rows.Scan(
			&i.RsID,
			&i.RsJobID,
			&i.JobVersion,
			&i.WaitlistVersion,
			&i.Position,
			&i.TravelDurationSeconds,
			&i.PlannedStartsAt,
			&i.PlannedEndsAt,
			&i.CurrentJobVersion,
			&i.WorkflowStatus,
			&i.ArchivedAt,
			&i.JobType,
			&i.TransportMode,
			&i.ExternalTransportConfirmed,
			&i.Latitude,
			&i.Longitude,
			&i.WaitlistID,
			&i.CurrentWaitlistVersion,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setRouteDraftAssigned = `-- name: SetRouteDraftAssigned :execrows
UPDATE route_drafts
SET status='assigned', assigned_at=now(), version=version+1, updated_at=now()
WHERE id=$1::uuid AND version=$2 AND status='draft'
`

type SetRouteDraftAssignedParams struct {
	ID              pgtype.UUID
	ExpectedVersion int32
}

func (q *Queries) SetRouteDraftAssigned(ctx context.Context, arg SetRouteDraftAssignedParams) (int64, error) {
	result, err := q.db.Exec(ctx, setRouteDraftAssigned, arg.ID, arg.ExpectedVersion)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateRouteDraft = `-- name: UpdateRouteDraft :execrows
UPDATE route_drafts
SET actor_user_id=$1::uuid,
    driver_id=$2::uuid,
    chipper_resource_id=$3::uuid,
    transport_resource_id=NULLIF($4::text, '')::uuid,
    departure_at=$5::timestamptz,
    start_label=$6,
    start_latitude=$7::numeric,
    start_longitude=$8::numeric,
    end_label=$9,
    end_latitude=$10::numeric,
    end_longitude=$11::numeric,
    routing_source=$12,
    distance_meters=$13,
    duration_seconds=$14,
    route_geometry=$15::jsonb,
    version=version+1,
    updated_at=now()
WHERE id=$16::uuid AND version=$17 AND status='draft'
`

type UpdateRouteDraftParams struct {
	ActorUserID         pgtype.UUID
	DriverID            pgtype.UUID
	ChipperResourceID   pgtype.UUID
	TransportResourceID string
	DepartureAt         pgtype.Timestamptz
	StartLabel          string
	StartLatitude       pgtype.Numeric
	StartLongitude      pgtype.Numeric
	EndLabel            string
	EndLatitude         pgtype.Numeric
	EndLongitude        pgtype.Numeric
	RoutingSource       string
	DistanceMeters      int32
	DurationSeconds     int32
	RouteGeometry       []byte
	ID                  pgtype.UUID
	ExpectedVersion     int32
}

func (q *Queries) UpdateRouteDraft(ctx context.Context, arg UpdateRouteDraftParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateRouteDraft,
		arg.ActorUserID,
		arg.DriverID,
		arg.ChipperResourceID,
		arg.TransportResourceID,
		arg.DepartureAt,
		arg.StartLabel,
		arg.StartLatitude,
		arg.StartLongitude,
		arg.EndLabel,
		arg.EndLatitude,
		arg.EndLongitude,
		arg.RoutingSource,
		arg.DistanceMeters,
		arg.DurationSeconds,
		arg.RouteGeometry,
		arg.ID,
		arg.ExpectedVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateRouteDraftMetrics = `-- name: UpdateRouteDraftMetrics :execrows
UPDATE route_drafts
SET routing_source=$1, distance_meters=$2,
    duration_seconds=$3, route_geometry=$4::jsonb,
    version=version+1, updated_at=now()
WHERE id=$5::uuid AND version=$6
  AND status='assigned'
`

type UpdateRouteDraftMetricsParams struct {
	RoutingSource   string
	DistanceMeters  int32
	DurationSeconds int32
	RouteGeometry   []byte
	ID              pgtype.UUID
	ExpectedVersion int32
}

func (q *Queries) UpdateRouteDraftMetrics(ctx context.Context, arg UpdateRouteDraftMetricsParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateRouteDraftMetrics,
		arg.RoutingSource,
		arg.DistanceMeters,
		arg.DurationSeconds,
		arg.RouteGeometry,
		arg.ID,
		arg.ExpectedVersion,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateRouteStopPosition = `-- name: UpdateRouteStopPosition :execrows
UPDATE route_stops
SET position=$1
WHERE id=$2::uuid AND route_draft_id=$3::uuid
`

type UpdateRouteStopPositionParams struct {
	Position     int32
	ID           pgtype.UUID
	RouteDraftID pgtype.UUID
}

func (q *Queries) UpdateRouteStopPosition(ctx context.Context, arg UpdateRouteStopPositionParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateRouteStopPosition, arg.Position, arg.ID, arg.RouteDraftID)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const updateRouteStopTravel = `-- name: UpdateRouteStopTravel :execrows
UPDATE route_stops
SET travel_distance_meters=$1,
    travel_duration_seconds=$2
WHERE id=$3::uuid AND route_draft_id=$4::uuid
`

type UpdateRouteStopTravelParams struct {
	TravelDistanceMeters  int32
	TravelDurationSeconds int32
	ID                    pgtype.UUID
	RouteDraftID          pgtype.UUID
}

func (q *Queries) UpdateRouteStopTravel(ctx context.Context, arg UpdateRouteStopTravelParams) (int64, error) {
	result, err := q.db.Exec(ctx, updateRouteStopTravel,
		arg.TravelDistanceMeters,
		arg.TravelDurationSeconds,
		arg.ID,
		arg.RouteDraftID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
