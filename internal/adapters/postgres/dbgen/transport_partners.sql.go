// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: transport_partners.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const insertTransportPartner = `-- name: InsertTransportPartner :one
INSERT INTO transport_partners (partner_type, name, phone, address, internal_note)
VALUES ($1, $2, NULLIF($3::text, ''),
        NULLIF($4::text, ''), NULLIF($5::text, ''))
RETURNING id::text
`

type InsertTransportPartnerParams struct {
	PartnerType  string
	Name         string
	Phone        string
	Address      string
	InternalNote string
}

func (q *Queries) InsertTransportPartner(ctx context.Context, arg InsertTransportPartnerParams) (string, error) {
	row := q.db.QueryRow(ctx, insertTransportPartner,
		arg.PartnerType,
		arg.Name,
		arg.Phone,
		arg.Address,
		arg.InternalNote,
	)
	var id string
	err := row.Scan(&id)
	return id, err
}

const listActiveTransportPartners = `-- name: ListActiveTransportPartners :many
SELECT id::text, partner_type, name, COALESCE(phone, '')::text AS phone,
       COALESCE(address, '')::text AS address, COALESCE(internal_note, '')::text AS internal_note,
       version, created_at, updated_at
FROM transport_partners
WHERE active
ORDER BY lower(name), id
`

type ListActiveTransportPartnersRow struct {
	ID           string
	PartnerType  string
	Name         string
	Phone        string
	Address      string
	InternalNote string
	Version      int32
	CreatedAt    pgtype.Timestamptz
	UpdatedAt    pgtype.Timestamptz
}

func (q *Queries) ListActiveTransportPartners(ctx context.Context) ([]ListActiveTransportPartnersRow, error) {
	rows, err := q.db.Query(ctx, listActiveTransportPartners)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListActiveTransportPartnersRow{}
	for rows.Next() {
		var i ListActiveTransportPartnersRow
		if err := rows.Scan(
			&i.ID,
			&i.PartnerType,
			&i.Name,
			&i.Phone,
			&i.Address,
			&i.InternalNote,
			&i.Version,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const lockActiveTransportPartner = `-- name: LockActiveTransportPartner :one
SELECT id::text
FROM transport_partners
WHERE id=$1::uuid AND active
FOR SHARE
`

func (q *Queries) LockActiveTransportPartner(ctx context.Context, id pgtype.UUID) (string, error) {
	row := q.db.QueryRow(ctx, lockActiveTransportPartner, id)
	var id_2 string
	err := row.Scan(&id_2)
	return id_2, err
}
